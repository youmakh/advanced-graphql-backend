# How To Launch

```bash
yarn install        # install modules
docker-compose up   # run prisma and mysql server
prisma generate     # generate prisma client
prisma deploy       # feed prisma server client's schema
node src/index.js   # run backend graphql server
```
